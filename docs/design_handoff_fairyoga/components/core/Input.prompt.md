Text input: 48px tall, sand background, 1px border, radius 12, label above with an 8px gap. Focus = teal border + soft teal ring. Error = danger border + danger-tint background + 13px message below.

```jsx
<Input label="Class name" placeholder="Vinyasa" />
<Input label="Email" error="Enter a valid email address" />
```
