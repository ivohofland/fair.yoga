The signature element on class cards: an 8px progress bar for registrations. Fill is danger until the minimum is met, teal after; a small ink tick marks the minimum; a tabular fraction ("8 / 6–14") sits above right.

```jsx
<RegistrationProgress registered={8} min={6} max={14} />
```
