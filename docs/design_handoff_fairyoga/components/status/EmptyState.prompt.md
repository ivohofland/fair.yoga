Empty state for list screens: exactly one subtitle, one body line, one action. No illustrations, no emoji.

```jsx
<EmptyState
  title="No classes this week"
  body="Pricing preview will appear when students register."
  actionLabel="+ Add class"
  onAction={create}
/>
```
