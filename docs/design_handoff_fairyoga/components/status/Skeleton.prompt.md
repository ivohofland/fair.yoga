Loading state: static sand blocks matching the layout (no shimmer, no spinners, no animation). Compose a few to mirror the real screen.

```jsx
<Skeleton height={96} radius={16} />
<Skeleton width="60%" height={16} style={{ marginTop: 12 }} />
```
