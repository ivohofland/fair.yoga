/**
 * Length of the waitlist's first-come-first-claimed window, which ends at
 * class start (#236).
 *
 * Free of imports, so a client component can import it directly.
 */
export const CLAIM_WINDOW_MINUTES = 60;
